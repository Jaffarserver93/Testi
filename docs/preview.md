# Preview the app locally

Run the app as a static site using Python's built-in server.

## Quick start

```bash
./scripts_preview.sh
```

This starts a local preview at:

- http://localhost:4173

## Custom port

```bash
./scripts_preview.sh 8080
```

Then open:

- http://localhost:8080
